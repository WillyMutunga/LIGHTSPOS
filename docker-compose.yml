version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: sparks_bytes_db
    restart: always
    environment:
      POSTGRES_DB: sparks_bytes
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
